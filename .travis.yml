language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"


# install deps
install:
    - pip install flake8 isort
    - pip install -r requirements.txt

before_script:
   - flake8 --show-source .
   - isort -c pyfirmata/*.py tests.py

# run tests
script: python tests.py
